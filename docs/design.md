# Design - v3


```shell
bm  # list the saved bookmarks
bm --list # list the saved bookmarks
bm --save # save new bookmark
bm --delete # delete the bookmark
bm --help # prints usage
bm --version  # prints version 
```